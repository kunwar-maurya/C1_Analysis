# AI Prompts — Debugging

## Prompt / instruction

Capture errors, root cause, fix, re-validate; maintain debugging-notes.md.

## Issues handled

1. Missing `python` on PATH → use `uv run`  
2. Duplicate-after-NULL count inflation → reorder injection  
3. Float AOV assertion → Decimal  

See `debugging-notes.md`.
